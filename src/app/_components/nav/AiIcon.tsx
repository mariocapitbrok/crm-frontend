import { Bot } from "lucide-react"
import { Button } from "@/components/ui/button"

const AiIcon = () => {
  return (
    <Button variant="ghost" size="icon" aria-label="Bot">
      <Bot />
    </Button>
  )
}

export default AiIcon
