export {
  type LeadOwner as User,
  type LeadRecord as Lead,
  useLeadOwners as useUsers,
  useLeadDirectory as useLeads,
  useCreateLeadEntry as useCreateLead,
  useUpdateLeadEntry as useUpdateLead,
} from "@/domains/leads"
