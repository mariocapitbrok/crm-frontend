export {
  type ContactRecord as Contact,
  useContactDirectory as useContacts,
} from "@/domains/contacts"
export {
  type OrganizationRecord as Organization,
  useOrganizationDirectory as useOrganizations,
} from "@/domains/organizations"
